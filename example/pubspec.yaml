name: ads_kit_example
description: "Demo app for the ads_kit mediation abstraction layer."
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  ads_kit:
    path: ../packages/ads_kit
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
